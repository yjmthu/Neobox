#define NEOBOX_VERSION "${VERSION}"
#define NEOBOX_VERSION_MAJOR ${VERSION_MAJOR}
#define NEOBOX_VERSION_MINOR ${VERSION_MINOR}
#define NEOBOX_VERSION_ALTER ${VERSION_ALTER}
#define NEOBOX_VERSION_BUILD ${VERSION_BUILD}

${define NEOBOX_BETA}
${define NEOBOX_LATEST}
${define NEOBOX_WEBSITE}
${define NEOBOX_COPYRIGHT}
